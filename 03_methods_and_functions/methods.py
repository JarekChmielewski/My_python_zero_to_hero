# methods perform specific actions on an object and can also take arguments object.method(arg1, arg2 ..)

#create a simple list
lst = [1, 2, 3, 4, 5, 2, 2]

lst.append(6)
print(lst)

# count the number of occurrences of element in the list
print(lst.count(2))

